// Configuration file for the application

// Use the environment variable VITE_API_URL if available, otherwise fallback to localhost
export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5055';
